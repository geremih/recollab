.row
  .col-lg-4
  .col-lg-4
    %h1.text-center Users

    %ul.list-group
      - @users.each do |user|
        %li.list-group-item.text-center
          = gravatar_for user
          %a{href: user_path(user) }
            = user.name
    = paginate @users






